apiVersion: v1
kind: Service
metadata:
  name: traefik-log-dashboard
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: traefik-log-dashboard
  ports:
    - name: http
      port: 3000
      targetPort: 3000
