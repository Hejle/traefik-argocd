apiVersion: v1
kind: Service
metadata:
  name: traefik-log-dashboard-agent
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: traefik-log-dashboard-agent
  ports:
    - name: http
      port: 5000
      targetPort: 5000
