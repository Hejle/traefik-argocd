apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: traefik-log-dashboard-data
  namespace: {{ .Values.namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
  storageClassName: local-path
